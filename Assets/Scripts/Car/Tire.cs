using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tire : MonoBehaviour
{
    private Vector3 accForces = Vector3.zero;

    private Vector3 suspension;
    private Vector3 acceleration;
    private Vector3 steering;

    //UpdateForce(), shoots ray and if ray hits update everything
    //GetForce()

    //UpdateSuspension, needs tire transform, springStrength, springRestDistance

    //UpdateAcceleration, 

    //UpdateSteering, needs tire transform, tireGripFactor 1 max grip 0 no grip

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
